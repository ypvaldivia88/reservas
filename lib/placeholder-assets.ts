/**
 * Re-exports for scripts that download optional local copies.
 * Runtime always uses curated Unsplash CDN URLs from placeholder-config.
 */
export {
  PLACEHOLDER_DOWNLOAD_URLS,
  listPlaceholderDownloadTargets,
} from "@/lib/placeholder-config";
